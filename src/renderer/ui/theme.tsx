import { createMuiTheme } from '@material-ui/core/styles';

const theme = createMuiTheme({
  palette: {
    primary: {
      main: '#344955'
    },
    secondary: {
      main: '#F9AA33',
    },
    background: {
      default: '#EDF0F2',
      paper: '#FEFEFE'
    }
  },
});

export default theme;
