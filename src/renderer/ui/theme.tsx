import { createTheme } from '@mui/material';

const theme = createTheme({
  palette: {
    primary: {
      main: '#344955'
    },
    secondary: {
      main: '#F9AA33',
    },
    background: {
      default: '#EDF0F2',
      paper: '#FEFEFE'
    }
  },
});

export default theme;
